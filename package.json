{
  "name": "fuji-recipes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Save Fujifilm film simulation recipes and send them to an X100VI over USB",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
